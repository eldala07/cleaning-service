import 'package:flutter/material.dart';

class SuccessfulOrderPage extends StatelessWidget {
  const SuccessfulOrderPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
